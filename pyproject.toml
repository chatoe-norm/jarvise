[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvise-ingest"
version = "0.1.0"
description = "Paper analytics ingest for Jarvise (no order placement)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
rag = [
  "qdrant-client>=1.9",
  "sentence-transformers>=3.0",
]

[project.scripts]
jarvise-ingest = "jarvise_ingest.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
